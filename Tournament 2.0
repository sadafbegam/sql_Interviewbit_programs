SELECT 
ROUND(
    SUM(CASE WHEN m.Result = 1 THEN 1 ELSE 0 END) / COUNT(*)
, 4) AS `Percentage Wins`
FROM Matches m
JOIN Team2 t2 ON m.PlayerId2 = t2.Id
WHERE t2.Cheater = 0
GROUP BY m.Date
ORDER BY m.Date ASC;
